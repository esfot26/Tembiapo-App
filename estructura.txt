TembiapoApp/
├── app/                            # Rutas controladas por expo-router
│   ├── _layout.tsx                 # Layout general (navbar/tab bar)
│   ├── index.tsx                   # Pantalla de inicio o Dashboard
│   ├── login/                      # Autenticación
│   │   ├── index.tsx               # Pantalla principal de login con Google
│   │   └── logout.tsx              # Pantalla opcional de cierre de sesión
│   └── (modals)/                   # Rutas modales
│       ├── nueva-tarea.tsx         # Modal para crear tarea rápida
│       └── nuevo-evento.tsx        # Modal para calendario
│
├── src/
│   ├── features/                   # Cada módulo o “feature” está aquí 👇
│   │   ├── auth/                   # 🔐 Módulo de autenticación (login con Google, contexto)
│   │   │   ├── components/         # Componentes específicos del login
│   │   │   │   └── GoogleButton.tsx
│   │   │   ├── hooks/
│   │   │   │   └── useAuth.ts      # Hook personalizado para el estado de sesión
│   │   │   ├── services/
│   │   │   │   ├── authService.ts  # Login, logout, persistencia Firebase
│   │   │   │   └── googleAuth.ts   # Integración directa con Expo AuthSession
│   │   │   ├── context/
│   │   │   │   └── AuthContext.tsx
│   │   │   └── types.ts            # Tipos o interfaces de usuario
│   │
│   │   ├── folders/                # 📂 Módulo de carpetas (organización tipo Drive)
│   │   │   ├── components/
│   │   │   │   ├── FolderCard.tsx  # Vista de carpeta individual
│   │   │   │   └── FolderList.tsx  # Lista de carpetas
│   │   │   ├── hooks/
│   │   │   │   └── useFolders.ts   # Hook que conecta con Firestore
│   │   │   ├── services/
│   │   │   │   └── foldersService.ts # CRUD en Firestore
│   │   │   ├── types.ts
│   │   │   └── index.tsx           # Pantalla principal de “Mis Carpetas”
│   │
│   │   ├── files/                  # 📄 Módulo de archivos (PDFs, imágenes, etc.)
│   │   │   ├── components/
│   │   │   │   ├── FileCard.tsx
│   │   │   │   └── UploadButton.tsx
│   │   │   ├── hooks/
│   │   │   │   └── useFiles.ts
│   │   │   ├── services/
│   │   │   │   └── filesService.ts # Manejo de Firebase Storage
│   │   │   └── index.tsx           # Pantalla dentro de una carpeta
│   │
│   │   ├── tasks/                  # ✅ Módulo To-Do List
│   │   │   ├── components/
│   │   │   │   ├── TaskCard.tsx
│   │   │   │   └── AddTaskForm.tsx
│   │   │   ├── hooks/
│   │   │   │   └── useTasks.ts
│   │   │   ├── services/
│   │   │   │   └── tasksService.ts # CRUD Firestore + notificaciones locales
│   │   │   ├── types.ts
│   │   │   └── index.tsx
│   │
│   │   ├── calendar/               # 🗓️ Módulo de calendario
│   │   │   ├── components/
│   │   │   │   ├── CalendarView.tsx
│   │   │   │   └── EventForm.tsx
│   │   │   ├── hooks/
│   │   │   │   └── useCalendar.ts
│   │   │   ├── services/
│   │   │   │   └── calendarService.ts
│   │   │   ├── types.ts
│   │   │   └── index.tsx
│   │
│   │   ├── notifications/          # 🔔 Módulo de notificaciones locales y push
│   │   │   ├── useNotifications.ts # Manejo de permisos, programación
│   │   │   └── scheduleReminder.ts # Función utilitaria
│   │
│   │   ├── profile/                # 👤 Perfil del usuario
│   │   │   ├── ProfileScreen.tsx
│   │   │   └── EditProfileForm.tsx
│   │
│   ├── shared/                     # Recursos reutilizables entre features
│   │   ├── components/             # Botones, modales, inputs genéricos
│   │   ├── hooks/                  # useTheme, useNetwork, etc.
│   │   ├── context/                # Contextos globales
│   │   ├── constants/              # Colores, tamaños, roles, etc.
│   │   ├── utils/                  # Helpers de formato, validación, fechas
│   │   └── types/                  # Tipos comunes (User, Folder, Task)
│
│   ├── services/                   # Servicios generales (Firebase, APIs externas)
│   │   ├── firebase.ts             # Configuración global de Firebase
│   │   └── storage.ts              # Inicialización y helpers de almacenamiento
│
│   └── theme/                      # Estilos globales (colores, tipografía, etc.)
│       └── theme.ts
│
├── assets/                         # Íconos, imágenes, fuentes
│   ├── icons/
│   ├── images/
│   └── fonts/
│
├── app.config.js                   # Configuración de Expo
├── babel.config.js
├── package.json
└── tsconfig.json                   # Si usas TypeScript



Estructura para el uso de vistas


TembiapoApp/
├── app/
│   ├── _layout.tsx
│   ├── index.tsx
│   └── login/
│       └── index.tsx           ✅ Enruta hacia src/features/login
│
└── src/
    └── features/
        └── login/
            └── index.tsx       🧩 Tu pantalla real de Login


//Ejemplo de _layout

import "@/global.css";
import { Stack, Redirect } from "expo-router";
import { StatusBar } from "expo-status-bar";
import { useColorScheme } from "nativewind";
import { ActiveScreenProvider } from "@/src/contexts/ActiveScreenContext";
import { AuthProvider, useAuth } from "@/src/contexts/AuthContext";
import { PortalHost } from "@rn-primitives/portal";
import Notifier from "@seybar/react-native-notifier";
import { DefaultTheme, DarkTheme } from "@react-navigation/native";
import { View, ActivityIndicator } from "react-native";

function LayoutContent() {
  const { colorScheme } = useColorScheme();
  const { usuario, loading } = useAuth();

  // ⏳ Mientras se verifica el estado del usuario
  if (loading) {
    return (
      <View
        style={{
          flex: 1,
          justifyContent: "center",
          alignItems: "center",
          backgroundColor:
            colorScheme === "dark"
              ? DarkTheme.colors.background
              : DefaultTheme.colors.background,
        }}
      >
        <ActivityIndicator size="large" color="#2563EB" />
        <StatusBar style={colorScheme === "dark" ? "light" : "dark"} />
      </View>
    );
  }

  // 🔐 Si no hay usuario → muestra login
  if (!usuario) {
    return (
      <Stack
        screenOptions={{
          headerShown: false,
          contentStyle: {
            backgroundColor:
              colorScheme === "dark"
                ? DarkTheme.colors.background
                : DefaultTheme.colors.background,
          },
        }}
      >
        <Stack.Screen name="login/index" />
        <Stack.Screen name="registro/index" />
      </Stack>
    );
  }

  // ✅ Si hay sesión → entra al inicio
  return (
    <Stack
      screenOptions={{
        headerShown: false,
        contentStyle: {
          backgroundColor:
            colorScheme === "dark"
              ? DarkTheme.colors.background
              : DefaultTheme.colors.background,
        },
      }}
    >
      <Stack.Screen name="inicio/index" />

    </Stack>
  );
}

export default function RootLayout() {
  return (
    <ActiveScreenProvider>
      <AuthProvider>
        <LayoutContent />
        <PortalHost />
        <Notifier.Container />
      </AuthProvider>
    </ActiveScreenProvider>
  );
}
